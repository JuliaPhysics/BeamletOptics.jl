# Development roadmap

In order to warrant a 1.0.0 release tag, the following features will need to be implemented. This definition is arbitrary. The exact timeframe for this development effort is not specified, but will be on the order of 1-2 years.

```@raw html
<RoadmapBoard />
```

## Get involved

```@raw html
<div class="bmo-card">
```

Every planned item above is up for grabs. See [Contributing](@ref) for the contribution
guidelines and the [issue tracker](https://github.com/JuliaPhysics/BeamletOptics.jl/issues)
for what is currently being discussed.

```@raw html
</div>
```
